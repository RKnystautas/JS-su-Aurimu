const miestai = [
    'Vilnius',
    'Klaipeda',
    'Siauliai'
];

miestai.forEach(example);

function example(miestas, index, array) {
    console.log(miestas);
    console.log(index);
    console.log(array);
}